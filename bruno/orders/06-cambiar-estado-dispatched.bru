meta {
  name: Cambiar Estado a DISPATCHED
  type: http
  seq: 6
}

patch {
  url: {{base_url}}/api/orders/1/status
  body: json
  auth: bearer
}

auth:bearer {
  token: {{operatorToken}}
}

body:json {
  {
    "status": "DISPATCHED"
  }
}
