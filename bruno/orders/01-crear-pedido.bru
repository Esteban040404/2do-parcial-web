meta {
  name: Crear Pedido - Exitoso
  type: http
  seq: 1
}

post {
  url: {{base_url}}/api/orders
  body: json
  auth: bearer
}

auth:bearer {
  token: {{operatorToken}}
}

body:json {
  {
    "items": [
      { "productId": 1, "quantity": 1 },
      { "productId": 3, "quantity": 3 }
    ]
  }
}
